# Device info (fill in once connected)

Run `scripts/check-device.sh` with the Fold3 connected via USB and ADB debugging enabled,
then paste the output below.

- Model:
- Build number / One UI version / Android version:
- Baseband/CSC (carrier):
- Bootloader state (`Download mode > OEM unlock` visibility):
- OEM unlocking toggle present in Developer Options?:
- Current bootloader/VaultKeeper lock status:
- Knox warranty bit status:

## Raw output

```
(paste adb/fastboot output here)
```
